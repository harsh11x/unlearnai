# Dependencies
node_modules/

# Environment (secrets)
.env
.env.local
.env.*.local

# Build files (too large for git — uploaded via upload-build.sh)
osapps/**/*.dmg
osapps/**/*.exe
osapps/**/*.msi
osapps/**/*.AppImage
osapps/**/*.deb
osapps/**/*.rpm
osapps/**/*.zip
builds/*.dmg
builds/*.exe
builds/*.zip

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db
