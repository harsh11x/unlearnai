# Dependencies
node_modules/

# Environment (secrets)
.env
.env.local
.env.*.local

# Build files (too large for git — uploaded via upload-build.sh)
builds/*.dmg
builds/*.exe
builds/*.zip
builds/*.AppImage

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db
